{
  // This is ctrl-shift-g off macOS and cmd-ctrl-shift-g on it. Every modifier
  // is explicit and the primary key stays lowercase.
  "lumine-workspace": {
    "cmdorctrl-ctrl-shift-g": "styleguide:show"
  }
}
